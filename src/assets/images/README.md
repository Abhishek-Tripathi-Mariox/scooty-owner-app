# Images Folder

Place app artwork here, for example:

- `movyra-logo.png`
- `login-hero.png`
- `otp-hero.png`

The current screens use a vector-style brand mark so the app still runs without image assets. Swap the mark in `src/components/BrandMark.tsx` to an `Image` when the PNGs are ready.
